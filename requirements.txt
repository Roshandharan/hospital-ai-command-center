# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Data validation
pydantic==2.7.1
pydantic-settings==2.2.1

# HTTP
httpx==0.27.0

# Logging
structlog==24.1.0

# Numerics (always required)
numpy==1.26.4

# ML — installed conditionally (train_models.py only, not required at runtime)
# xgboost>=2.0.0
# shap>=0.45.0
# scikit-learn>=1.4.0
# pandas>=2.0.0

# RAG — optional, graceful fallback if missing
# chromadb>=0.5.0
# sentence-transformers>=2.7.0

# LLM — optional, fallback plan activates if missing
# anthropic>=0.30.0

# LangGraph — optional, falls back to sequential execution
# langgraph>=0.2.0

# Database — optional, only needed when DATABASE_URL is set
# sqlalchemy[asyncio]>=2.0.0
# asyncpg>=0.29.0
